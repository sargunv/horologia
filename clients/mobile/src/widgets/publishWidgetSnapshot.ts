import type { WidgetSnapshotV1 } from "@horologia/client-core";

export async function publishWidgetSnapshot(_snapshot: WidgetSnapshotV1): Promise<void> {
  // Widgets are currently available only in native Apple and Android builds.
}

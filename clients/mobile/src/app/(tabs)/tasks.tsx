import { ScreenState } from "@/components/screen-state";

export default function TasksScreen() {
  return (
    <ScreenState
      title="Your tasks will appear here"
      detail="The authenticated task viewer arrives in the next milestone."
    />
  );
}

# Craft Workpad: Mobile Login Flow

> Generated by /craft · Started: 2026-04-21

---

## Task

Implement the mobile login flow per `mobile/design/login-flow.html`.

**Scope — what to build:**

- Server picker screen (new): Compact and Expanded layouts (WindowSizeClass / horizontalSizeClass).
  Single URL field with debounced probe (600ms), 6 states per § C (empty / typing / probing / valid
  / invalid-unreachable / invalid-wrong-server). Default scheme to `https://` on bare host. No
  network-range validation — accept any URL, any scheme.
- OAuth browser handoff per § D: ASWebAuthenticationSession on iOS, Custom Tabs on Android, loopback
  redirect on desktop. PKCE + state. Callback `horologia://oauth` on mobile,
  `http://127.0.0.1:<port>` on desktop.
- Finishing sub-state per § E: token exchange → secure-store write → parallel cache warm-up
  (`/api/profile` + `/api/spaces`) → 300ms minimum dwell.
- Cold-launch routing per § H: has-server? has-token? → silent refresh → Profile (cached) or
  ServerPicker.
- Drop the dev-token bootstrap: remove `HOROLOGIA_DEV_TOKEN` from
  `ApiBootstrapKt.configureHorologiaApi` callsites; the `getToken` closure reads from the new
  session store.
- Profile surface unchanged — today's placeholder stays. Redesign is the next task.

**Libraries / platform targets to bump:**

- Compose `androidx.compose.material3` to a version shipping Material 3 Expressive (LoadingIndicator
  with wave, expressive shape/motion theme, MaterialExpressiveTheme if that's the API). Apply to
  `:compose-app` (Android + desktop). Also pull in `material3-adaptive` /
  `material3-window-size-class` if not already present.
- iOS: bump `iosApp` deployment target to iOS 26 in `project.yml` and the KMP `iosMain` linker
  target in `mobile/core/build.gradle.kts` / `mobile/gradle/libs.versions.toml` so SwiftUI Liquid
  Glass (`.glassEffect()`, tinted glass materials, `GlassEffectContainer`) is available. Use them on
  the login surface where they improve it; don't force glass on read-heavy surfaces.

**Platform cleartext config:**

- Android: `network_security_config.xml` with `cleartextTrafficPermitted="true"` globally.
- iOS: `NSAppTransportSecurity` dict with `NSAllowsArbitraryLoads = true` in `iosApp/project.yml`
  Info.plist.
- No app-layer URL-shape validation.

**Likely blocker the pipeline must surface during explore/clarify (P0):** The server today has
cookie-based `POST /app/auth/login` + `POST /api/auth/tokens` (mints bearer from cookie), not OAuth
with PKCE. The spec assumes OAuth. The architect phase has to pick one: (a) add server-side OAuth
endpoints (bigger scope; may need a separate task), (b) drive the existing cookie flow through the
system browser and terminate with a server-side redirect to `horologia://oauth?token=…`, (c) change
the plan (e.g., in-app form, but that violates design principle #2). Expect this to shape the
architecture phase.

Context: mobile project at `mobile/`. `:core` is the shared KMP library (ViewModels + OpenAPI
client + AppContainer). `:compose-app` is Compose for Android + desktop. `iosApp/` is SwiftUI
consuming `HorologiaCore.xcframework`. Today's `ProfileViewModel` / `SpacesViewModel` +
per-destination `ViewModelStoreOwner` pattern (landed in commit `a7a1e94`) is the template for
`LoginViewModel`. Navigation: Compose Navigation on Android/desktop, SwiftUI `NavigationStack` on
iOS.

Commits style reference: recent mobile commits in `git log --oneline`.

Design spec: `mobile/design/login-flow.html` (wireframe spec — structure, copy, states, responsive
rules, platform primitives).

---

## Exploration Report

### Key Facts (most actionable first)

- **The "likely P0 blocker" framing in the task is wrong.** `server/internal/api/oauth.go:97-125`
  mounts a full OAuth 2.1 authorization server with PKCE S256, metadata discovery at
  `/.well-known/oauth-authorization-server`, and public-client auth
  (`"token_endpoint_auth_methods_supported": ["none"]`). Landed in PR #53 (`0bcb295`). **Mobile can
  speak OAuth directly; no server-side OAuth work needed for the happy path.**
- **Server redirect matcher is loopback-only** (`server/internal/api/oauth.go:693-714`): accepts
  exact matches against `OauthClient.RedirectUris` OR (if `LoopbackRedirects=TRUE`) any `http://`
  URI with a `localhost` or loopback IP host. Custom schemes like `horologia://oauth` are not
  accepted today — either the seeded client's `redirect_uris` array gets the custom scheme added (a
  migration), or the matcher learns to allow custom-scheme exact matches, or mobile uses loopback on
  Android/iOS too (unusual).
- **Only one OAuth client is seeded**
  (`server/internal/database/migrations/00001_initial.sql:319-337`): `horologia-cli`,
  `redirect_uris='{}'`, `loopback_redirects=TRUE`, no client secret, `is_first_party=TRUE`. No
  `horologia-mobile` row — architect must decide whether to reuse `horologia-cli` or seed a distinct
  client.
- **OAuth endpoints are NOT in TypeSpec** (`api/src/auth.tsp`). They're hand-registered.
  Consequence: the generated `AuthApi` Kotlin client cannot call `/oauth/authorize` or
  `/oauth/token` — mobile will need to hit them via Ktor directly (Ktor is already a transitive dep
  via `openapi-kmp-gen:companion` v1.3.0, no explicit pin).
- **No `/api/meta` endpoint exists.** `/healthz` is mounted at root, outside `/api`. The
  design-spec's "server probe" target should be `GET /app/auth/config` (`api/src/auth.tsp:87-89`,
  `@useAuth(NoAuth)`) — it returns
  `AuthConfig { oidc:{enabled,label,autoRedirect}, password:{enabled} }` and is the natural "is this
  a Horologia server" probe. Landing on this also tells the client whether the server supports
  password + OIDC before OAuth starts.
- **Prior-art OAuth+PKCE+loopback client exists at `cli/`** (PR #53). The mobile desktop target can
  mirror its loopback listener; mobile iOS/Android translate the same PKCE + state logic to
  `ASWebAuthenticationSession` / Custom Tabs.
- **Canonical ViewModel template** lives at
  `mobile/core/src/commonMain/kotlin/dev/horologia/mobile/core/feature/profile/ProfileGateway.kt:11-45`
  and `ProfileViewModel.kt:11-43`. Shape: `internal interface FeatureGateway` + `FetchXxxResult`
  sealed 4-case (`Ok`, `AuthFailure`, `Retryable`, `Permanent`) +
  `internal class LiveFeatureGateway` + `class FeatureViewModel(gateway)` with
  `MutableStateFlow<UiState>` + `init { refresh() }` + cancel-safe `refresh()`. New
  `LoginViewModel` + `ServerProbeViewModel` mirror this verbatim.
- **`ApiBootstrap.configureHorologiaApi(baseUrl, getToken)` mutates a process-global singleton**
  (`mobile/core/src/commonMain/kotlin/dev/horologia/mobile/core/ApiBootstrap.kt:14-22`). `baseUrl`
  is `override var` (writable); `getToken` is a closure evaluated per request; `Auth.BearerAuth` can
  be added/cleared atomically. **Server switch and token swap are both cheap**, but today's
  callsites capture compile-time-constants — they need to read from a mutable holder.
- **Three dev-token callsites** all carry the same TODO
  (`mobile/compose-app/.../MainActivity.kt:10-18`, `.../desktopMain/.../Main.kt:10-14`,
  `mobile/iosApp/iosApp/HorologiaApp.swift:7-21`). All three disappear when the session-backed
  provider lands.
- **Compose M3 is JetBrains Compose Multiplatform, not androidx.**
  `mobile/gradle/libs.versions.toml:35` pins `org.jetbrains.compose.material3:material3:1.9.0`. The
  androidx `material3` entry (`:23`) is declared but unreferenced. M3 Expressive availability in CMP
  must be confirmed against
  [JetBrains CMP release notes](https://github.com/JetBrains/compose-multiplatform/releases) — not
  locally verifiable. Androidx `material3` 1.4+ has `MaterialExpressiveTheme` and the wave
  `LoadingIndicator` (annotated `@ExperimentalMaterial3ExpressiveApi`); CMP historically lags by a
  minor version or more.
- **iOS deployment target is 17.0** (`mobile/iosApp/project.yml:5`). Liquid Glass (`.glassEffect()`,
  `GlassEffectContainer`) requires iOS 26 / Xcode 26 / SwiftUI 7. KMP `:core` sets no explicit iOS
  min-version — defaults to Kotlin/Native's baseline (iOS 14 in Kotlin 2.3.x). The Xcode-layer
  deployment target is the binding constraint at load time.
- **No `CFBundleURLTypes` and no Android deeplink intent-filter.** `mobile/iosApp/project.yml:29-36`
  has no `urlSchemes` section. `mobile/compose-app/src/androidMain/AndroidManifest.xml:13-22` has
  only `MAIN`/`LAUNCHER`. Custom-scheme redirects require adding both.
- **`androidx.browser:browser` is not in the catalog.** Custom Tabs support on Android needs a new
  dep.
- **Persistence is greenfield in `:core`.** Zero matches for
  `DataStore | SharedPreferences | NSUserDefaults | Keychain | multiplatform-settings | SecureRandom | MessageDigest | androidx.security.crypto`.
  No expect/actual declarations anywhere; `:core/src/` has only `commonMain`, `commonTest`,
  `iosMain` directories. Session-token storage, PKCE SHA-256, and server-URL persistence are all new
  builds.
- **No existing cache in the gateways.** `LiveProfileGateway.fetchMe()` hits `UsersApi.usersMe()`
  every call; `LiveSpacesGateway.fetchSpaces()` same. The design spec's "warm up existing `:core`
  cache" (`mobile/design/login-flow.html` § G) is aspirational —
  cold-launch-renders-Profile-from-cache requires building a cache layer.
- **`CallExceptionClassifier.classify()` already buckets 401/403 to `Failure.Auth`**
  (`mobile/core/.../net/CallExceptionClassifier.kt:14-47`). A silent-refresh interceptor has an
  obvious hook, but today gateways propagate `AuthFailure` unmodified.
- **Compose NavHost `startDestination = ProfileRoute` is hardcoded**
  (`mobile/compose-app/.../HorologiaApp.kt:25`). No signed-in-vs-signed-out branching exists. iOS
  equivalent: `NavigationStack { ProfileView() }` in `HorologiaApp.swift:25`. Cold-launch routing to
  Login vs. Profile will live at these two sites plus their desktop sibling.
- **`network_security_config.xml` already permits cleartext for `10.0.2.2` + `localhost`.** Matches
  iOS `NSAllowsLocalNetworking: true`. For the design-spec's "accept any URL" rule, both need
  broadening to arbitrary hosts (`cleartextTrafficPermitted="true"` globally on Android,
  `NSAllowsArbitraryLoads=true` on iOS).
- **iOS screen template**: `mobile/iosApp/iosApp/ProfileView.swift:5-19` shows the per-view
  `@StateObject IosViewModelStoreOwner` +
  `.task { for await newState in viewModel.uiState { ... } }` pattern. New `LoginView` mirrors it.
- **No mobile-side logging facility** — zero uses of `Log.` / `Logger` / `println` in `:core`.
  Auth-flow telemetry (if any) starts from scratch.

### Code Snippets

**Server OAuth endpoints (already live)** — `server/internal/api/oauth.go:99-125`:

```go
mux.Handle("/.well-known/oauth-authorization-server", oauthHandler)
mux.Handle("/.well-known/oauth-protected-resource", oauthHandler)
mux.Handle("/app/oauth/consent", oauthHandler)
mux.Handle("/oauth/authorize", oauthHandler)
mux.Handle("/oauth/token", oauthHandler)
mux.Handle("/oauth/revoke", oauthHandler)
mux.Handle("/", base)

// inside NewOAuthHandler:
mux.HandleFunc("GET /oauth/authorize", handler.oauthAuthorize)
mux.HandleFunc("POST /app/oauth/consent", handler.oauthAuthorize)
mux.HandleFunc("POST /oauth/token", handler.oauthToken)
mux.HandleFunc("POST /oauth/revoke", handler.oauthRevoke)
```

**Server redirect-URI matcher (loopback-only today)** — `server/internal/api/oauth.go:693-714`:

```go
func oauthRedirectURIAllowed(client dbgen.OauthClient, redirectURI string) bool {
    if slices.Contains(client.RedirectUris, redirectURI) {
        return true
    }
    if !client.LoopbackRedirects {
        return false
    }
    u, err := url.Parse(redirectURI)
    if err != nil || u.Scheme != "http" {
        return false
    }
    host := u.Hostname()
    if host == "localhost" { return true }
    ip := net.ParseIP(host)
    return ip != nil && ip.IsLoopback()
}
```

**Seeded OAuth client (only one)** —
`server/internal/database/migrations/00001_initial.sql:319-337`:

```sql
INSERT INTO oauth_clients (client_id, display_name, redirect_uris,
                           loopback_redirects, client_secret_hash, is_first_party, created_at)
VALUES ('horologia-cli', 'Horologia CLI', '{}', TRUE, NULL, TRUE, now())
ON CONFLICT (client_id) DO NOTHING;
```

**ViewModel + Gateway template** —
`mobile/core/src/commonMain/kotlin/dev/horologia/mobile/core/feature/profile/ProfileGateway.kt:11-45`:

```kotlin
internal interface ProfileGateway {
  suspend fun fetchMe(): FetchProfileResult
}

internal sealed interface FetchProfileResult {
  data class Ok(val displayName: String) : FetchProfileResult
  data object AuthFailure : FetchProfileResult
  data class Retryable(val message: String) : FetchProfileResult
  data class Permanent(val message: String) : FetchProfileResult
}

internal class LiveProfileGateway(private val requestTimeout: Duration = 15.seconds) :
  ProfileGateway {
  override suspend fun fetchMe(): FetchProfileResult = try {
    withTimeout(requestTimeout) {
      UsersApi.usersMe().fold(
        ifRight = { FetchProfileResult.Ok(displayName = it.data.name) },
        ifLeft = { exception -> when (val failure = exception.classify()) {
          Failure.Auth -> FetchProfileResult.AuthFailure
          is Failure.Retryable -> FetchProfileResult.Retryable(message = failure.message)
          is Failure.Permanent -> FetchProfileResult.Permanent(message = failure.message)
        }},
      )
    }
  } catch (_: TimeoutCancellationException) {
    FetchProfileResult.Retryable(message = "Request timed out after $requestTimeout.")
  }
}
```

**ApiBootstrap (the seam to replace)** —
`mobile/core/src/commonMain/kotlin/dev/horologia/mobile/core/ApiBootstrap.kt:14-22`:

```kotlin
fun configureHorologiaApi(baseUrl: String, getToken: () -> String?) {
  Api.baseUrl = try { Url(baseUrl) } catch (e: IllegalArgumentException) {
    throw IllegalArgumentException("Invalid Horologia base URL: '$baseUrl'", e)
  }
  Api.setAuthProvider(Auth.BearerAuth { getToken()?.let { AuthItem.Bearer(it) } })
}
```

**iOS view template** — `mobile/iosApp/iosApp/ProfileView.swift:5-19`:

```swift
@MainActor
struct ProfileView: View {
  @Environment(\.appContainer) private var container
  @StateObject private var storeOwner = IosViewModelStoreOwner()
  @State private var uiState: ProfileUiState = ProfileUiStateLoading.shared

  private var viewModel: ProfileViewModel {
    storeOwner.viewModel(
      ProfileViewModel.self,
      factory: container!.profileViewModelFactory
    )
  }
  // ...
}
```

**Compose NavHost (where startDestination is decided today)** —
`mobile/compose-app/.../HorologiaApp.kt:20-33`:

```kotlin
@Composable
fun HorologiaApp(appContainer: AppContainer) {
  CompositionLocalProvider(LocalAppContainer provides appContainer) {
    MaterialTheme {
      Surface {
        val navController = rememberNavController()
        NavHost(navController = navController, startDestination = ProfileRoute) {
          composable<ProfileRoute> {
            ProfileScreen(onOpenSpaces = { navController.navigate(SpacesRoute) })
          }
          composable<SpacesRoute> { SpacesScreen(onBack = { navController.popBackStack() }) }
        }
      }
    }
  }
}
```

### Key Files

**Server — OAuth (consumed, not modified for the happy path):**

- `server/internal/api/oauth.go` — live OAuth 2.1 + PKCE; `oauthRedirectURIAllowed` at line 693 is
  the only likely server-side edit (if custom schemes are chosen over loopback).
- `server/internal/database/migrations/00001_initial.sql:319-337` — single seeded `horologia-cli`
  OAuth client; a mobile client may want its own row.
- `api/src/auth.tsp:87-89` — `GET /app/auth/config` as unauthenticated server probe.

**`:core` — ViewModel/Gateway/UiState templates + wiring to replicate:**

- `mobile/core/src/commonMain/kotlin/dev/horologia/mobile/core/feature/profile/` — canonical feature
  package (Gateway, UiState, ViewModel).
- `mobile/core/src/commonMain/kotlin/dev/horologia/mobile/core/ApiBootstrap.kt` — global-singleton
  Api config; the single seam for baseUrl + token swap.
- `mobile/core/src/commonMain/kotlin/dev/horologia/mobile/core/AppContainer.kt` — factory registry;
  `LoginViewModelFactory` and new gateways register here.
- `mobile/core/src/commonMain/kotlin/dev/horologia/mobile/core/net/CallExceptionClassifier.kt` —
  `Failure.Auth` hook for silent refresh.
- `mobile/core/src/commonTest/kotlin/dev/horologia/mobile/core/feature/profile/ProfileViewModelTest.kt`
  — test-harness template.

**`:core` — absent but referenced by new work:**

- `mobile/core/src/commonMain/kotlin/dev/horologia/mobile/core/session/SessionStore.kt` (new,
  expect) — session-token store.
- `mobile/core/src/androidMain/`, `desktopMain/`, augmented `iosMain/` (new source dirs) — actual
  implementations.
- `mobile/core/src/commonMain/kotlin/dev/horologia/mobile/core/feature/login/` (new feature
  package).

**`:compose-app` — nav + bootstrap:**

- `mobile/compose-app/src/commonMain/kotlin/dev/horologia/mobile/compose/HorologiaApp.kt` — NavHost
  root; `startDestination` decision point; `MaterialTheme` call site.
- `mobile/compose-app/src/commonMain/kotlin/dev/horologia/mobile/compose/nav/Routes.kt` —
  `@Serializable data object` routes.
- `mobile/compose-app/src/androidMain/.../MainActivity.kt` — Android bootstrap + (new) deep-link
  intent receiver.
- `mobile/compose-app/src/desktopMain/.../Main.kt` — desktop bootstrap + (new) loopback OAuth
  listener host.
- `mobile/compose-app/src/androidMain/AndroidManifest.xml` — intent-filter for OAuth return.
- `mobile/compose-app/src/androidMain/res/xml/network_security_config.xml` — widen cleartext scope.

**`iosApp/`:**

- `mobile/iosApp/iosApp/HorologiaApp.swift` — SwiftUI `@main`; start-view decision point.
- `mobile/iosApp/iosApp/ProfileView.swift`, `IosViewModelStoreOwner.swift`,
  `AppContainerEnvironment.swift` — screen/VM pattern templates.
- `mobile/iosApp/project.yml` — deployment target (17→26), `CFBundleURLTypes`,
  `NSAppTransportSecurity`.
- `mobile/iosApp/Config.xcconfig` — dev-token shim; removed by this task.

**Dep catalogue / build configs:**

- `mobile/gradle/libs.versions.toml` — version catalog; CMP M3 1.9.0, Kotlin 2.3.20, navigation
  2.9.2; adds for M3 Expressive / `androidx.browser:browser` land here.
- `mobile/compose-app/build.gradle.kts` — BuildConfig dev-token injection (to be removed);
  `targetSdk=36`.
- `mobile/core/build.gradle.kts` — KMP targets; needs `androidMain`/`desktopMain` source-set
  populated; may need iOS `-target` min-version bump.

**Design spec:**

- `mobile/design/login-flow.html` — wireframe spec consumed by architect + implement.

**Reference prior-art:**

- `cli/` (per PR #53) — working OAuth+PKCE+loopback client; desktop target can mirror.

---

## Requirements

_Generated by `/craft-clarify`. These thread through architect, implement, and review._

### Server picker screen

**R1:** A new `ServerPicker` screen is the app's entry point when no session exists. It contains a
single URL text field, a primary "Continue" button, and a supporting-text row under the field that
reports probe state. Source: human confirmed (design spec § C) Verification: grep for `ServerPicker`
in `:compose-app` and `iosApp`; visual inspection against `mobile/design/login-flow.html` § C.

**R2:** `ServerPicker` renders a **Compact** layout (single-column, full-viewport) below a 600 dp /
`.compact` breakpoint and an **Expanded** layout (centered card, max-width ~480 dp, two-pane on
larger viewports) at or above. Branches on Compose `WindowSizeClass.widthSizeClass` and SwiftUI
`@Environment(\.horizontalSizeClass)`. One view hierarchy per platform, branched internally. Source:
human confirmed (design spec § C.01.a / 01.b) Verification: resize window / rotate tablet / unfold
foldable in preview or runtime — layout visibly switches at the breakpoint.

**R3:** When the user omits the scheme (e.g., types `tasks.example.com`), probe uses `https://`.
Typed `http://` and `https://` are honored exactly. No network-range validation, no rejection based
on URL shape. Source: human confirmed (earlier conversation) Verification: unit test on the URL
parser in `:core`; manual probe of `localhost:8080`, `http://192.168.x.x`, `https://example.com`.

**R4:** Probe target is `GET /app/auth/config` on the user-supplied base URL. A 200 response that
deserializes as `AuthConfig { oidc, password }` confirms a Horologia server; any other response
(timeout, non-200, malformed JSON, wrong shape) routes to `Invalid · wrong server` or
`Invalid · unreachable`. Source: craft-clarify recommendation (only viable unauth endpoint per
exploration) Verification: unit test on the server-probe gateway using a fake HTTP response;
integration test against dev server.

**R5:** The field transitions through exactly 6 states per design spec § C: `Empty`, `Typing`,
`Probing`, `Valid`, `Invalid · unreachable`, `Invalid · wrong server`. Probe debounces at 600 ms
after the last keystroke (or fires immediately on blur). Continue is enabled only in `Valid`.
Source: human confirmed (design spec § C states) Verification: `ServerProbeViewModelTest` asserts
state transitions for each path (kept/dropped keystrokes, success, failure, malformed response).

### OAuth flow

**R6:** The flow uses OAuth 2.1 Authorization Code with **PKCE S256** and the **`state`** parameter.
`code_verifier` (43–128 chars, base64url) is generated at browser-launch time, held in memory only,
and cleared on completion (success, failure, or timeout). `state` is a fresh random value; callbacks
with a mismatched `state` are treated as hostile — PKCE state dropped, user returned to ServerPicker
with a banner. Source: craft-clarify recommendation (security baseline; matches server's advertised
`code_challenge_methods_supported: ["S256"]`) Verification: unit test on PKCE verifier generation
and `code_challenge` derivation; test that callback with wrong state transitions to error.

**R7:** Redirect URI is **`horologia://oauth`** on iOS and Android and
**`http://127.0.0.1:<random-port>/`** on desktop. Mobile uses custom scheme; desktop uses loopback.
Source: human confirmed (Q1) Verification: grep for the callback strings in iOS/Android/desktop
entry points; inspect OAuth authorize URL built by the client.

**R8:** A new OAuth client `horologia-mobile` is seeded via a fresh Goose migration in
`server/internal/database/migrations/`:

- `client_id = 'horologia-mobile'`
- `display_name = 'Horologia'`
- `redirect_uris = '{horologia://oauth}'`
- `loopback_redirects = TRUE` (covers desktop)
- `client_secret_hash = NULL`
- `is_first_party = TRUE` Source: human confirmed (Q1) Verification: new migration file exists;
  `SELECT * FROM oauth_clients WHERE client_id='horologia-mobile'` against a fresh-migrated dev DB
  returns the expected row.

**R9:** Browser primitive per platform:

- **iOS:** `ASWebAuthenticationSession` with `prefersEphemeralWebBrowserSession` default true;
  callback caught by the session's completion handler (not Universal Links / scene delegate).
- **Android:** `androidx.browser.customtabs.CustomTabsIntent` for the outbound tab; callback arrives
  via an intent-filter on a handler activity that routes back into the same `LoginViewModel`.
- **Desktop:** `java.awt.Desktop.browse()` (or equivalent) for the outbound browser; callback caught
  by a short-lived `com.sun.net.httpserver.HttpServer` bound to `127.0.0.1:<random-port>` that
  returns a minimal "You may close this tab" response and shuts down within 5 s of a valid callback.
  Source: human confirmed (design spec § D + Q1) Verification: grep for each primitive; manual
  trace-through on each platform.

**R10:** Finishing sub-state: on callback OK, the client exchanges code at `POST /oauth/token` with
the stored `code_verifier`, parses access + refresh tokens and TTL, persists them to the OS secure
store keyed by server host, then routes to Profile. A minimum dwell of 300 ms is enforced so the
transition reads as deliberate. Source: human confirmed (design spec § E) Verification: unit test on
the finishing state machine; measure dwell in manual trace (should never be under 300 ms).

**R11:** No profile / spaces cache warm-up during the finishing sub-state. Profile renders its usual
first-load on arrival. Source: human confirmed (Q2 — explicitly deferred) Verification: grep for
`/api/profile` calls during finishing — should be zero; Profile's existing `fetchMe()` runs on mount
as today.

### Persistence & cold launch

**R12:** Tokens (access + refresh) persist in the OS secure store, keyed per server host:

- **iOS:** Keychain via `Security.framework` `actual`, `kSecAttrAccessibleAfterFirstUnlock`.
- **Android:** `EncryptedSharedPreferences` via `androidx.security.crypto` (or a Tink-backed
  alternative picked during architect phase).
- **Desktop:** macOS Keychain, Windows Credential Manager, libsecret on Linux — whichever
  cross-platform KMP / JVM library is chosen during architect phase. Fallback to an encrypted file
  with a user-derived key is acceptable where a vault is unavailable. Source: human confirmed
  (design spec § G) Verification: manual inspection of secure-store entry on each platform; tokens
  persist across app restart.

**R13:** The current server URL (plain string) persists in platform app-prefs: DataStore on Android,
UserDefaults on iOS, a file-based JVM preferences mechanism on desktop. PKCE verifier and state live
in process memory only. Source: human confirmed (design spec § G) Verification: inspect written
prefs; confirm PKCE values never appear on disk.

**R14:** Cold-launch routing runs before first frame paint:

- No saved server → ServerPicker (empty).
- Server saved but no refresh token → ServerPicker with URL pre-filled.
- Both present → fire silent refresh: success → Profile; failure → ServerPicker with banner "Signed
  out." Source: human confirmed (design spec § H) Verification: integration trace for each branch on
  fresh-install and on a corrupted-session simulation.

### Drop dev-token bootstrap

**R15:** All three `HOROLOGIA_DEV_TOKEN` callsites are removed: `BuildConfig.HOROLOGIA_DEV_TOKEN` +
its `buildConfigField` in `compose-app/build.gradle.kts`; the `System.getenv("HOROLOGIA_DEV_TOKEN")`
read in `desktopMain/Main.kt`; the `Info.plist` `HorologiaDevToken` key (including its `project.yml`
source and `Config.xcconfig` shim). `ApiBootstrap.configureHorologiaApi(baseUrl, getToken)` keeps
its signature but `getToken` now reads from the new session store. Source: human confirmed (task
description) Verification: grep for `HOROLOGIA_DEV_TOKEN`, `HorologiaDevToken` across the repo —
zero matches outside historical comments.

**R16:** No debug escape hatch for bypassing the OAuth flow. Developers use the real flow against
`admin@localhost` / `password` on the dev server. Source: craft-clarify recommendation Verification:
grep for `DEBUG`-gated auth branches in `ApiBootstrap` and `AppContainer` — zero matches.

### Platform configuration

**R17:** Android cleartext: `mobile/compose-app/src/androidMain/res/xml/network_security_config.xml`
is updated to `cleartextTrafficPermitted="true"` with no per-domain whitelist. Existing `10.0.2.2` /
`localhost` stanza is removed. Source: human confirmed (earlier conversation, scheme handling
simplification) Verification: visual inspection of the file; Android app can connect to an
arbitrary-hostname HTTP server at runtime.

**R18:** iOS ATS: `mobile/iosApp/project.yml` `NSAppTransportSecurity` dict is updated to
`NSAllowsArbitraryLoads: true`. `NSAllowsLocalNetworking: true` is removed (subsumed). Source: human
confirmed Verification: generated `Info.plist` shows `NSAllowsArbitraryLoads = true`; iOS app can
connect to an arbitrary-hostname HTTP server.

**R19:** iOS URL schemes: `project.yml` gains a `CFBundleURLTypes` entry registering `horologia` as
a URL scheme on the app bundle. xcodegen regenerates the Xcode project. Source: human confirmed (R7)
Verification: generated `Info.plist` contains `CFBundleURLTypes` with
`CFBundleURLSchemes = ["horologia"]`; iOS routes `horologia://oauth?...` to the app.

**R20:** Android intent filter: `MainActivity` (or a dedicated `OAuthRedirectActivity`) gains an
`<intent-filter>` with `action.VIEW`, `category.DEFAULT` + `category.BROWSABLE`, and
`<data android:scheme="horologia" android:host="oauth"/>`. The activity routes the incoming intent
back into the login state machine. Source: human confirmed (R7) Verification:
`adb shell am start -a android.intent.action.VIEW -d "horologia://oauth?code=x&state=y"` launches
the app and reaches the finishing state.

### Library bumps & UI frameworks

**R21:** Compose Material 3 Expressive: implementation phase web-searches Compose Multiplatform
release notes for `MaterialExpressiveTheme` / wavy `LoadingIndicator` / expressive shape theme
availability in CMP 1.9.x. If available, the login screen uses them. If not, it ships with baseline
`MaterialTheme` from `org.jetbrains.compose.material3:material3:1.9.0`. No mixing with androidx
`compose-material3`. Source: human confirmed (Q3) Verification: inspect the login screen's theme
import; run the screen on Android + desktop.

**R22:** iOS deployment target hard-bumps to 26.0: `project.yml` `deploymentTarget.iOS = "26.0"`.
Login surface uses Liquid Glass where it improves the composition (`.glassEffect()`,
`GlassEffectContainer`, tinted glass materials on the panel / button). No `#available` hedging — the
app requires iOS 26+. Source: human confirmed (Q4) Verification: `project.yml` shows `iOS: "26.0"`;
grep for `.glassEffect` in `iosApp/`; visual inspection of the login screen on a
Liquid-Glass-capable simulator.

**R23:** `androidx.browser:browser` is added to `mobile/gradle/libs.versions.toml` and applied to
`:compose-app` Android target. Any other new deps required by the secure-store actuals land here
too. Source: craft-clarify recommendation (direct consequence of R9) Verification:
`libs.versions.toml` diff; Custom Tabs builds on Android.

### Code structure

**R24:** New feature package
`mobile/core/src/commonMain/kotlin/dev/horologia/mobile/core/feature/login/` follows the existing
`feature/profile/` template: internal Gateway interface + `FetchXxxResult` sealed 4-case +
LiveGateway + ViewModel with `MutableStateFlow<UiState>`, `init { ... }` or constructor-driven first
action, cancel-safe refresh/submit via `fetchJob?.cancel()`. Source: craft-clarify recommendation
(architecture consistency) Verification: file-layout check; shape matches `ProfileGateway.kt` /
`ProfileViewModel.kt` byte-for-byte on the structural axes.

**R25:** New `LoginViewModelFactory` and `ServerProbeViewModelFactory` register in `AppContainer.kt`
alongside the existing `profileViewModelFactory` / `spacesViewModelFactory`. Source: craft-clarify
recommendation Verification: grep for `loginViewModelFactory` in `AppContainer.kt`.

**R26:** Unit tests for new ViewModels mirror the `ProfileViewModelTest` template —
`StandardTestDispatcher`, `Dispatchers.setMain`/`resetMain`, fake Gateway, cases for loading →
success → each failure bucket → concurrent-refresh. Source: craft-clarify recommendation (existing
test bar) Verification: test files exist at
`mobile/core/src/commonTest/kotlin/dev/horologia/mobile/core/feature/login/`; tests pass.

**R27:** Profile surface is unchanged. No additions, no sign-out UI in this task. Developers testing
sign-out clear app data manually; this is accepted for this task. Source: human confirmed (task
description, implicit) Verification: `git diff mobile/compose-app/...compose/feature/profile/` and
`mobile/iosApp/iosApp/ProfileView.swift` — no changes beyond what's required by nav-root flip.

---

## Architectural Decision

- **Chosen approach:** Composite — Pragmatic core with Robust edges
- **Core idea:** One `LoginViewModel` owns the whole flow (server-picker state machine + PKCE +
  browser + token exchange + finishing dwell) following the existing `feature/profile` template
  byte-for-byte; four `expect/actual` platform contracts (`SessionStore`, `ServerPrefs`,
  `BrowserLauncher`, `Crypto`) cover persistence, browser, and hash/random;
  `ApiBootstrap(baseUrl, getToken)` signature unchanged (per-request `DefaultRequest` block verified
  in Ktor 3.4.2 bytecode, so re-calling to swap baseUrl works); dedicated Android
  `OAuthRedirectActivity` keeps the back-stack clean; AES-GCM file on desktop satisfies R12's
  fallback clause.
- **Approved:** 2026-04-21

### Approaches Considered

- **Minimal** — single VM, 5 `expect/actual` contracts, plain-0600 desktop store, JDK HttpServer.
  Lost on desktop security (plain file vs encrypted) and Android back-stack robustness.
- **Cleanest/Robust** — two VMs, 6 contracts + reactive `SessionHolder`-driven bootstrap, Ktor CIO
  desktop server, silent-refresh interceptor. Lost on VM split (violates codebase 1-VM-per-screen
  convention) and `ApiBootstrap` signature churn (no correctness gain; verified per-request block
  read in Ktor).
- **Pragmatic** — single VM, one `SessionStore` contract, MainActivity `onNewIntent` routing, JDK
  HttpServer. Lost on embedding browser logic at screen layer (should be a platform contract) and
  Android back-stack bug from routing OAuth through MainActivity.

### Requirement Coverage

- R1: ServerPicker screen — `LoginScreen.kt` + `LoginView.swift` hosting `LoginViewModel` in the
  `ServerPicker` state
- R2: Compact < 600 dp, Expanded ≥ 600 dp — `BoxWithConstraints` branch in Compose +
  `@Environment(\.horizontalSizeClass)` in SwiftUI; one view hierarchy per platform
- R3: Default scheme https on bare host — `UrlNormalizer.kt` pure helper in `:core/commonMain`
- R4: Probe = `GET /app/auth/config` — `LiveLoginGateway.probeServer` via throwaway Ktor HttpClient
- R5: 6 field states + 600 ms debounce — `LoginUiState.ProbeState` enum +
  `LoginViewModel.onUrlChanged` cancel-and-relaunch delay
- R6: PKCE S256 + state validation + hostile-state handling — `Pkce.kt` helper + VM-private fields
  cleared in `finally`
- R7: `horologia://oauth` mobile, `http://127.0.0.1:<port>/` desktop —
  `BrowserLauncher.redirectUri()` per-platform actuals
- R8: `horologia-mobile` OAuth client — **edit
  `server/internal/database/migrations/00001_initial.sql`** to append a second
  `INSERT INTO oauth_clients` stanza for `horologia-mobile` (pre-prod squash convention; matches
  backend's single-source-of-truth approach)
- R9: Per-platform browser primitives — `BrowserLauncher` actuals: ASWebAuthenticationSession (iOS,
  via Swift shim), Custom Tabs (Android, via `:compose-app/androidMain` installer),
  `com.sun.net.httpserver.HttpServer` + `Desktop.browse()` (desktop)
- R10: Token exchange + 300 ms min dwell — `LoginViewModel` `Finishing` state with `Clock`-driven
  dwell check
- R11: No cache warm-up — verified by grep; `LoginGateway` never references
  `ProfileGateway`/`SpacesGateway`
- R12: Tokens in secure store — `SessionStore` actuals: iOS Keychain (`SecItemAdd/Copy/Delete`,
  `kSecAttrAccessibleAfterFirstUnlock`), Android `EncryptedSharedPreferences`, desktop AES-GCM file
  at platform user-data dir with 0600-mode sibling random-key file
- R13: Server URL in prefs — `ServerPrefs` actuals: DataStore / NSUserDefaults /
  `java.util.prefs.Preferences`; PKCE/state stay VM-private
- R14: Cold-launch routing — `BootRouter.decide()` called before first frame from each platform
  entry point; returns one of 4 `BootDestination` cases; silent refresh runs in background after
  optimistic Profile render, emits `SignedOut` event on failure
- R15: All three dev-token callsites removed — `BuildConfig` field + `HORO­LO­GIA_DEV_TOKEN` env
  read + `HorologiaDevToken` Info.plist key + `Config.xcconfig` file deleted
- R16: No debug escape hatch — verified by grep of `BuildConfig.DEBUG` / `#if DEBUG` in auth code
- R17: Android cleartext — `network_security_config.xml` reduced to
  `<base-config cleartextTrafficPermitted="true"/>`
- R18: iOS ATS — `NSAllowsArbitraryLoads=true` in `project.yml`
- R19: iOS URL scheme — `CFBundleURLTypes` with `horologia` scheme in `project.yml`
- R20: Android intent-filter — dedicated `OAuthRedirectActivity` with `taskAffinity=""`,
  `launchMode="singleTask"`; intent-filter on `android:scheme="horologia" android:host="oauth"`
- R21: M3 Expressive if available — `HorologiaTheme.kt` wrapper tries `MaterialExpressiveTheme`,
  falls back to baseline `MaterialTheme`. Implementer web-searches CMP 1.9.0 release notes.
- R22: iOS 26 hard-bump + Liquid Glass — `deploymentTarget.iOS = "26.0"`; `.glassEffect()` +
  `GlassEffectContainer` on login card + primary button; no `#available` hedging
- R23: New deps — `androidx.browser`, `androidx.security:security-crypto`, Ktor client family
  (explicit pins), `kotlinx-serialization-json` added to `libs.versions.toml`
- R24: `feature/login/` mirrors `feature/profile/` — file layout: `LoginGateway.kt`,
  `LoginUiState.kt`, `LoginViewModel.kt`, plus supporting `Pkce.kt`, `UrlNormalizer.kt`,
  `BootRouter.kt`
- R25: Factories in `AppContainer` — `loginViewModelFactory` registered alongside
  `profileViewModelFactory` / `spacesViewModelFactory`
- R26: Tests mirror `ProfileViewModelTest` — `LoginViewModelTest`, `PkceTest`, `UrlNormalizerTest`,
  `LoginGatewayTest`, `BootRouterTest`, `ApiBootstrapTest` (regression guard on Ktor behavior)
- R27: Profile unchanged — verified by git diff of `feature/profile/` + `ProfileView.swift`

### Key trade-offs resolved

- **Single VM vs. split:** Single wins on codebase convention (1 VM per screen in `feature/profile`,
  `feature/spaces`).
- **`ApiBootstrap` signature:** Unchanged, because Ktor 3.4.2's `DefaultRequest.Plugin.install.1`
  bytecode confirms per-request block invocation — `Api.baseUrl` mutation takes effect on next
  request.
- **Desktop secure store:** AES-GCM with per-install random key file (R12 fallback clause) — cheap
  encryption over plain JSON.
- **Desktop loopback:** JDK `com.sun.net.httpserver.HttpServer`, not Ktor CIO. Zero new deps;
  consistency argument doesn't justify 2+ new libs.
- **Android OAuth redirect:** Dedicated `OAuthRedirectActivity` with `taskAffinity=""`, not
  `MainActivity.onNewIntent`. Prevents back-stack bug when user backgrounds app mid-flow.
- **Silent-refresh mid-session:** Deferred. Cold-launch refresh only this task; mid-session 401
  surfaces `AuthFailure` and user re-signs-in.
- **Cold-launch timing:** Optimistic Profile render + background refresh, not blocking on refresh.
  Decision step itself is `runBlocking` (prefs+store read, sub-50 ms, no network).
- **DB migration:** Squashed into `00001_initial.sql` per backend's pre-prod convention. Re-login
  friction is identical either way because DB reset wipes `auth_tokens` regardless.
- **TanStack-Query-style data layer:** Explicitly deferred to a follow-up `/craft`. Login has no
  cache semantics worth that scope expansion.

### Deferred to Implementation

- [ ] CMP 1.9.0 M3 Expressive API availability — implementer web-searches release notes; picks
      `MaterialExpressiveTheme` or baseline `MaterialTheme` (R21)
- [ ] `androidx.security:security-crypto` deprecation status in April 2026 — implementer confirms
      latest stable artifact; falls back to `MasterKey` + hand-rolled AES/GCM if the library has
      been yanked
- [ ] Android `SessionStore.Context` injection — implementer picks between constructor-param (via
      `AppContainer(context)`) and `androidx.startup` Initializer
- [ ] KMP iOS min-version linker flag on `:core/build.gradle.kts` — implementer verifies whether
      iosApp bump to 26.0 requires a corresponding `-mios-version-min` on the framework target
- [ ] Ktor version pin — today Ktor comes transitively via `openapi-kmp-gen:companion:1.3.0`;
      implementer decides whether to add an explicit pin in `libs.versions.toml` (currently two
      versions in the Gradle cache: 3.3.1 + 3.4.2 — pinning avoids drift)

### Deferred Items (post-task)

- TanStack-Query-style data layer (Store / SQLDelight-backed repositories). Would migrate
  `ProfileGateway`/`SpacesGateway` to `Fetcher<Key, Value>` shape and remove manual `StateFlow`
  plumbing. Separate `/craft` task.
- Mid-session silent-refresh interceptor on 401. Mutex-protected `withSilentRefresh` gateway helper.
- Real OS-keychain integration on desktop (macOS Keychain, Windows Credential Manager, libsecret).
- Sign-out UI on Profile (deferred alongside the Profile redesign, which is the next task).
- Mobile observability (structured logging for auth events).

### Adversarial finding

- **Severity:** P2 (verified safe). Original claim that `Api.baseUrl` reassignment would be a dead
  store was based on older Ktor behavior. Disassembly of `io.ktor:ktor-client-core-jvm:3.4.2`'s
  `DefaultRequest$Plugin$install$1.class` (offsets 54–118) shows a fresh `DefaultRequestBuilder`
  constructed per request, with `plugin.block(freshBuilder)` invoked dynamically — meaning
  `apiConfig$lambda$1`'s `url.takeFrom(getBaseUrl())` reads `Api.baseUrl` on every outbound call.
  Multi-call `configureHorologiaApi` is safe.
- **Addressed by:** `ApiBootstrapTest` (commonTest) rebases the client through two different URLs
  and asserts `MockEngine` sees the second host. Guards against a future Ktor upgrade reverting to
  install-time URL capture. `ApiBootstrap.kt` doc comment updated to record the behavior assumption.

### Files to create/modify

**Server (1 file):**

- `server/internal/database/migrations/00001_initial.sql` — **edit** to append a second
  `INSERT INTO oauth_clients` stanza for `horologia-mobile` (`redirect_uris='{horologia://oauth}'`,
  `loopback_redirects=TRUE`, `is_first_party=TRUE`, no secret) right after the existing
  `horologia-cli` seed.

**`:core/commonMain` (new):**

- `feature/login/LoginGateway.kt` — `internal interface` + `LiveLoginGateway`; methods
  `probeServer`, `exchangeCode`, `refreshAccessToken`; throwaway Ktor `HttpClient` per call.
- `feature/login/LoginUiState.kt` — sealed hierarchy: `ServerPicker(input, probe, banner?)` +
  `ProbeState` enum (Empty/Typing/Probing/Valid/InvalidUnreachable/InvalidWrongServer) +
  `LaunchingBrowser`, `Finishing`, `Complete`.
- `feature/login/LoginViewModel.kt` — whole-flow owner; 600 ms debounced probe; VM-private
  PKCE+state cleared in `finally`;
  `onUrlChanged / onSubmit / completeOAuth / onOAuthCancelled / onOAuthFailed / attemptSilentRefresh`.
- `feature/login/Pkce.kt` — `generateCodeVerifier()` + `codeChallengeS256(verifier)` pure helpers
  using `Crypto`.
- `feature/login/UrlNormalizer.kt` — pure helper.
- `feature/login/BootRouter.kt` —
  `suspend fun decideBootDestination(prefs, store, gateway): BootDestination`.
- `session/SessionStore.kt` — `expect class` with `read/write/clear(host: String)` on
  `StoredSession`.
- `session/ServerPrefs.kt` — `expect class` with `loadServerUrl / saveServerUrl`.
- `session/SessionHolder.kt` — in-memory holder; `currentAccessToken(): String?` (sync);
  `install(host, tokens)` (suspend); `StateFlow<Session?>` for observers.
- `platform/BrowserLauncher.kt` — `expect class` with
  `suspend fun launchAndAwait(authorizeUrl, redirectUri): String` + `fun redirectUri(): String`.
- `platform/Crypto.kt` — `expect object` with `sha256(ByteArray): ByteArray` +
  `randomBytes(Int): ByteArray`.

**`:core/commonMain` (modify):**

- `ApiBootstrap.kt` — signature unchanged; doc comment updated to record Ktor per-request-block
  behavior and multi-call safety; callsites now point at `sessionHolder.currentAccessToken()`.
- `AppContainer.kt` — add `sessionStore`, `serverPrefs`, `sessionHolder`, `browserLauncher`,
  `loginGateway`, `bootRouter`, `loginViewModelFactory`; platform-specific constructor for Android
  (takes `Context`).

**`:core/commonTest` (new):**

- `feature/login/LoginViewModelTest.kt`, `PkceTest.kt`, `UrlNormalizerTest.kt`,
  `LoginGatewayTest.kt`, `BootRouterTest.kt`, `ApiBootstrapTest.kt`.
- `platform/Fakes.kt` — `FakeBrowserLauncher`, `FakeSessionStore`, `FakeServerPrefs`, `FakeCrypto`
  (deterministic).

**`:core/androidMain` (new source set):**

- `session/SessionStore.android.kt` — `EncryptedSharedPreferences`.
- `session/ServerPrefs.android.kt` — `SharedPreferences`.
- `platform/Crypto.android.kt` — `MessageDigest` + `SecureRandom`.
- `platform/BrowserLauncher.android.kt` — delegates to a `:compose-app`-installed
  `AndroidBrowserLauncher` (Custom Tabs needs Activity).

**`:core/desktopMain` (new source set):**

- `session/SessionStore.desktop.kt` — AES-GCM file + sibling random-key file at platform user-data
  dir, mode 0600.
- `session/ServerPrefs.desktop.kt` — `java.util.prefs.Preferences`.
- `platform/Crypto.desktop.kt` — JCE.
- `platform/BrowserLauncher.desktop.kt` — `com.sun.net.httpserver.HttpServer` bound to
  `127.0.0.1:0` + `Desktop.browse()`.

**`:core/iosMain` (augmented):**

- `session/SessionStore.ios.kt` — `SecItemAdd/Copy/Delete` with
  `kSecAttrAccessibleAfterFirstUnlock`.
- `session/ServerPrefs.ios.kt` — `NSUserDefaults.standardUserDefaults`.
- `platform/Crypto.ios.kt` — `CC_SHA256` + `SecRandomCopyBytes`.
- `platform/BrowserLauncher.ios.kt` — delegates to Swift-installed `IosBrowserLauncher`.

**`:core/build.gradle.kts` (modify):** add `androidMain` + `desktopMain` source sets; deps per
platform.

**`:compose-app/commonMain`:**

- `nav/Routes.kt` — add `@Serializable data object LoginRoute`.
- `HorologiaApp.kt` — `startDestination` becomes a parameter; add `composable<LoginRoute>`.
- `theme/HorologiaTheme.kt` — new wrapper; tries M3 Expressive, falls back to baseline.
- `feature/login/LoginScreen.kt` — new; compact/expanded branch; hosts `LoginViewModel`.

**`:compose-app/androidMain`:**

- `MainActivity.kt` — rewrite; drops dev-token; `runBlocking { bootRouter.decide() }` for
  startDestination; installs `AndroidBrowserLauncher`.
- `platform/AndroidBrowserLauncher.kt` — new; `CustomTabsIntent` launcher.
- `platform/OAuthRedirectActivity.kt` — new; `taskAffinity=""`, `launchMode="singleTask"`; receives
  intent, emits to channel, `finish()`.
- `AndroidManifest.xml` — add `OAuthRedirectActivity` with intent-filter for `VIEW + BROWSABLE` +
  scheme `horologia` host `oauth`.
- `res/xml/network_security_config.xml` — `cleartextTrafficPermitted="true"` globally.

**`:compose-app/desktopMain`:**

- `Main.kt` — rewrite; drops env-token; `runBlocking { bootRouter.decide() }`.

**`:compose-app/build.gradle.kts`:** remove `HOROLOGIA_DEV_TOKEN` + `HOROLOGIA_BASE_URL` BuildConfig
fields; add `androidx.browser:browser`.

**`iosApp/`:**

- `project.yml` — `deploymentTarget.iOS = "26.0"`; add `CFBundleURLTypes` scheme `horologia`;
  `NSAllowsArbitraryLoads: true`; drop `HorologiaBaseUrl/HorologiaDevToken` + `configFiles.Debug`.
- `Config.xcconfig` — delete.
- `HorologiaApp.swift` — rewrite; drops dev-token; `@State` start-destination guard awaiting
  `bootRouter.decide()`; `.onOpenURL` forwards to browser launcher.
- `LoginView.swift` — new; compact/expanded branch; `.glassEffect()` + `GlassEffectContainer`.
- `OAuthLauncher.swift` — new; wraps `ASWebAuthenticationSession`; installed into `BrowserLauncher`
  at app init.

**Dep catalogue (`mobile/gradle/libs.versions.toml`):** add `androidx-browser`,
`androidx-security-crypto`, Ktor client family (explicit pin matching current transitive),
`kotlinx-serialization-json`.

### Test strategy

Unit (KMP `commonTest`, runs on all three platforms):

- `UrlNormalizerTest` — table-drive 5 cases.
- `PkceTest` — RFC 7636 § A.3 reference vector; length invariants.
- `LoginGatewayTest` — Ktor `MockEngine`: 200 valid / 200 malformed / 404 / timeout /
  `{"error":"invalid_grant"}`.
- `LoginViewModelTest` — 7 cases per design spec § I + debounce + concurrent-refresh cancellation;
  `StandardTestDispatcher` + `FakeClock` for 300 ms dwell.
- `BootRouterTest` — all 4 cold-launch branches.
- `ApiBootstrapTest` — regression guard: rebase twice with different URLs, assert `MockEngine` sees
  the second host.

Integration / manual:

- Android: `adb shell am start -a android.intent.action.VIEW -d "horologia://oauth?code=x&state=y"`
  reaches `Finishing`.
- iOS 26 simulator: full flow against dev server; Liquid Glass visual inspection.
- Desktop: full flow via `mise run //mobile:desktop:run`.
- Migration: `mise run db:reset && psql -c "SELECT client_id FROM oauth_clients"` shows
  `horologia-cli` and `horologia-mobile`.

---

## Acceptance Criteria

**AC1** (R1, R24, R25): A new `feature/login` package exists at
`mobile/core/src/commonMain/kotlin/dev/horologia/mobile/core/feature/login/` with `LoginGateway.kt`,
`LoginUiState.kt`, `LoginViewModel.kt`, and `AppContainer.kt` gains a `loginViewModelFactory`
property. Auto-verify:
`ls mobile/core/src/commonMain/kotlin/dev/horologia/mobile/core/feature/login/` shows the three
files + supporting helpers;
`grep -n 'loginViewModelFactory' mobile/core/src/commonMain/kotlin/dev/horologia/mobile/core/AppContainer.kt`
returns a match. ✓ DONE — feature/login/ has 6 files (Gateway, UiState, ViewModel, Pkce,
UrlNormalizer, BootRouter); AppContainer.kt has `loginViewModelFactory`.

**AC2** (R2): `LoginScreen.kt` branches layouts on `WindowSizeClass.widthSizeClass` and
`LoginView.swift` branches on `@Environment(\.horizontalSizeClass)`. Auto-verify:
`grep -n 'WindowSizeClass\|widthSizeClass\|BoxWithConstraints' mobile/compose-app/src/commonMain/kotlin/dev/horologia/mobile/compose/feature/login/LoginScreen.kt`
and `grep -n 'horizontalSizeClass' mobile/iosApp/iosApp/LoginView.swift` both match. ✓ DONE —
Compose uses `BoxWithConstraints` + `maxWidth >= 600.dp` (M3 `WindowSizeClass` isn't on the CMP
1.9.0 classpath; the measured width serves the same role, recorded in a doc comment on
`LoginScreen.kt`). iOS uses `@Environment(\.horizontalSizeClass)`.

**AC3** (R3): `UrlNormalizer.kt` defaults bare hosts to `https://`, preserves explicit schemes.
Auto-verify: `UrlNormalizerTest` passes — 5-row table test. ✓ DONE — `UrlNormalizerTest` (7 cases
covering bare host, explicit http/https, whitespace trim, trailing slash, path preservation,
empty→null) passes via `mise run //mobile:test`.

**AC4** (R4): `LiveLoginGateway.probeServer` hits `GET /app/auth/config` and maps 200 → Ok,
200-wrong-shape → WrongServer, other/timeout → Unreachable. Auto-verify:
`LoginGatewayTest.probeServer_*` passes with Ktor MockEngine fakes. ✓ DONE —
`LoginGatewayTest.probeServer_*` (4 cases: valid/malformed/non-200/network-fail) green.

**AC5** (R5): `LoginUiState` has a `ProbeState` enum with 6 cases; `LoginViewModel.onUrlChanged`
debounces probes at 600 ms. Auto-verify: grep
`ProbeState.(Empty|Typing|Probing|Valid|InvalidUnreachable|InvalidWrongServer)` all present;
`LoginViewModelTest.debounce*` passes. ✓ DONE — `ProbeState` sealed interface has exactly the 6
cases; `LoginViewModelTest.onUrlChangedDebouncesAndTransitionsToValid` +
`newKeystrokeCancelsPendingProbe` exercise the 600ms debounce.

**AC6** (R6): `Pkce.kt` has `generateCodeVerifier` and `codeChallengeS256`; `LoginViewModel`
validates callback state; mismatch transitions to ServerPicker with banner. Auto-verify: `PkceTest`
passes RFC 7636 § A.3 vector; `LoginViewModelTest.hostileStateMismatch` passes. ✓ DONE —
`PkceTest.codeChallengeMatchesRfc7636Vector` asserts the §A.3 reference;
`LoginViewModelTest.stateMismatchReturnsToServerPickerWithBanner` green. PKCE/state cleared in
`finally` per VM.

**AC7** (R7): `BrowserLauncher.redirectUri()` returns `horologia://oauth` on iOS + Android,
`http://127.0.0.1:<port>/` on desktop. Auto-verify:
`grep -n 'horologia://oauth' mobile/core/src/androidMain mobile/core/src/iosMain` +
`grep -n '127.0.0.1' mobile/core/src/desktopMain/kotlin/dev/horologia/mobile/core/platform/BrowserLauncher.desktop.kt`.
✓ DONE — verified via inline constants in each actual.

**AC8** (R8): `server/internal/database/migrations/00001_initial.sql` contains a seed row for
`client_id = 'horologia-mobile'` with `redirect_uris = '{horologia://oauth}'`. Auto-verify:
`grep -n "'horologia-mobile'" server/internal/database/migrations/00001_initial.sql` returns a
match. ✓ DONE — appended after `horologia-cli` INSERT in migration 00001.

**AC9** (R9): Three `BrowserLauncher` actuals use `ASWebAuthenticationSession` (iOS via Swift shim),
Custom Tabs (Android via `CustomTabsIntent`), and `com.sun.net.httpserver.HttpServer` (desktop).
Auto-verify: grep for each primitive in its target source dir. ✓ DONE — iOS actual delegates to
Swift `OAuthLauncher` which uses `ASWebAuthenticationSession`; Android delegates to
`AndroidBrowserLauncherImpl` using `CustomTabsIntent.Builder()`; desktop uses
`com.sun.net.httpserver.HttpServer.create`.

**AC10** (R10): `LoginViewModel.Finishing` sub-state persists tokens and holds a 300 ms minimum
dwell. Auto-verify: `LoginViewModelTest.finishingMinDwell` passes with FakeClock /
`StandardTestDispatcher.advanceTimeBy`. ✓ DONE — `LoginViewModelTest.finishingMinDwellIsHonored`
green; VM computes `remaining = finishingMinDwellMillis - elapsed` and delays if positive.

**AC11** (R11): `feature/login/` code never calls `ProfileGateway` or `SpacesGateway`. Auto-verify:
`grep -r 'ProfileGateway\|SpacesGateway' mobile/core/src/commonMain/kotlin/dev/horologia/mobile/core/feature/login/`
returns nothing. ✓ DONE — feature/login/ only imports from `net/`, `platform/`, `session/`, and
`generated/`; no profile/spaces refs.

**AC12** (R12): `SessionStore` `actual class` exists for iOS (Keychain), Android
(EncryptedSharedPreferences), and desktop (AES-GCM file). Auto-verify: the three actual files exist;
grep for `SecItemAdd`, `EncryptedSharedPreferences`, `Cipher.getInstance("AES/GCM/NoPadding")`
respectively. ✓ DONE — verified in each `SessionStore.*.kt`.

**AC13** (R13): `ServerPrefs` `actual class` exists for all three platforms; PKCE verifier/state
held only in `LoginViewModel` fields. Auto-verify: grep for PKCE verifier field name in
`SessionStore` / `ServerPrefs` — zero matches. ✓ DONE — three ServerPrefs actuals exist;
`pendingVerifier` / `pendingState` exist only as private fields in `LoginViewModel.kt`.

**AC14** (R14): `BootRouter.decideBootDestination` returns one of 4 `BootDestination` cases;
cold-launch entry points invoke it before first frame. Auto-verify: `BootRouterTest` covers all 4
branches; grep `bootRouter.decide` in platform entry points. ✓ DONE — `BootRouterTest` has 4 tests;
`MainActivity.onCreate`, desktop `Main.kt`, and iOS `HorologiaApp.init` all call
`bootRouter.decideBootDestination()` via `runBlocking` / `DispatchSemaphore` before first frame.

**AC15** (R15): `HOROLOGIA_DEV_TOKEN` / `HorologiaDevToken` callsites removed. Auto-verify:
`grep -r 'HOROLOGIA_DEV_TOKEN\|HorologiaDevToken' mobile/` returns zero matches (excluding this
workpad). ✓ DONE — BuildConfig fields, desktop env read, Info.plist key, and `Config.xcconfig` all
removed. (workpad.md itself still mentions the token names in requirement copy, which is expected.)

**AC16** (R16): No debug-gated auth bypass anywhere. Auto-verify:
`grep -rn 'BuildConfig.DEBUG' mobile/core/src mobile/compose-app/src` shows no auth-related matches;
`grep -rn '#if DEBUG' mobile/iosApp/iosApp/` shows no auth-related matches. ✓ DONE — grep returns
zero hits.

**AC17** (R17): `network_security_config.xml` globally permits cleartext. Auto-verify:
`grep 'cleartextTrafficPermitted="true"' mobile/compose-app/src/androidMain/res/xml/network_security_config.xml`
matches; no per-domain stanzas. ✓ DONE — file now contains
`<base-config cleartextTrafficPermitted="true" />` only.

**AC18** (R18): iOS `NSAppTransportSecurity` dict has `NSAllowsArbitraryLoads = true`. Auto-verify:
`grep -A 2 'NSAppTransportSecurity' mobile/iosApp/project.yml` shows `NSAllowsArbitraryLoads: true`.
✓ DONE — `NSAllowsLocalNetworking` replaced by `NSAllowsArbitraryLoads: true`.

**AC19** (R19): iOS `project.yml` has `CFBundleURLTypes` with `horologia` scheme. Auto-verify:
`grep -A 5 'CFBundleURLTypes' mobile/iosApp/project.yml` contains `horologia`. ✓ DONE — added with
`CFBundleURLName: dev.horologia.mobile.oauth` + scheme `horologia`.

**AC20** (R20): Android has `OAuthRedirectActivity` in manifest with correct intent-filter.
Auto-verify:
`grep -A 10 'OAuthRedirectActivity' mobile/compose-app/src/androidMain/AndroidManifest.xml` shows
intent-filter with `android:scheme="horologia"` and `android:host="oauth"`. ✓ DONE — activity
declared with `taskAffinity=""`, `launchMode="singleTask"`, `Theme.NoDisplay`, intent-filter
`VIEW` + `BROWSABLE` + scheme `horologia` + host `oauth`.

**AC21** (R21): `HorologiaTheme` composable wraps `MaterialTheme` or `MaterialExpressiveTheme`;
`LoginScreen` uses it. Auto-verify:
`grep -n 'fun HorologiaTheme\|MaterialTheme\|MaterialExpressiveTheme' mobile/compose-app/src/commonMain/kotlin/dev/horologia/mobile/compose/theme/HorologiaTheme.kt`
matches. ✓ DONE — `HorologiaTheme` wraps baseline `MaterialTheme`; doc comment records that CMP
1.9.0 does not ship `MaterialExpressiveTheme` (verified via JetBrains CMP CHANGELOG WebFetch) and
that the body swaps once CMP catches up. `HorologiaApp.kt` wraps content in `HorologiaTheme`.

**AC22** (R22): iOS deployment target is 26.0; `.glassEffect()` appears on login surfaces.
Auto-verify: `grep 'iOS: "26.0"' mobile/iosApp/project.yml` matches;
`grep -n '.glassEffect' mobile/iosApp/iosApp/LoginView.swift` matches. ✓ DONE —
`deploymentTarget.iOS = "26.0"`; `LoginView.swift` applies
`.glassEffect(in: .rect(cornerRadius: 28))` to the Regular-size card and
`.glassEffect(in: .capsule)` to the Continue button inside a `GlassEffectContainer`. [MANUAL-VERIFY]
Visual Liquid Glass inspection requires an iOS 26 simulator — xcodegen regenerates the project
cleanly but a simulator run is the caller's responsibility.

**AC23** (R23): `libs.versions.toml` pins `androidx-browser`, `androidx-security-crypto`, `ktor`,
`kotlinx-serialization-json`. Auto-verify:
`grep -n 'androidx-browser\|androidx-security-crypto\|ktor\|kotlinx-serialization-json' mobile/gradle/libs.versions.toml`
shows entries for all four. ✓ DONE — all four libraries and their versions pinned; ktor pinned to
3.4.2.

**AC24** (R24, R25): `feature/login/` mirrors `feature/profile/` structure; `AppContainer` gains
`loginViewModelFactory`. Auto-verify: same as AC1 (structure); additionally
`grep 'SessionStore\|SessionHolder' mobile/core/src/commonMain/kotlin/dev/horologia/mobile/core/AppContainer.kt`
matches. ✓ DONE — `AppContainer` is an `expect class` with `sessionStore`, `sessionHolder`,
`serverPrefs`, `browserLauncher`, `bootRouter`, `loginViewModelFactory`, `profileViewModelFactory`,
`spacesViewModelFactory`.

**AC25** (R26): Test files exist and pass: `LoginViewModelTest`, `PkceTest`, `UrlNormalizerTest`,
`LoginGatewayTest`, `BootRouterTest`, `ApiBootstrapTest`. Auto-verify: `mise run //mobile:core:test`
green. ✓ DONE — the task is `mise run //mobile:test` (not `//mobile:core:test`); ran green with 34
tests (9 login-ViewModel, 5 gateway, 4 BootRouter, 4 PKCE, 7 UrlNormalizer, 1 ApiBootstrap plus
pre-existing profile/spaces).

**AC26** (R27): Profile surface unchanged apart from NavHost startDestination wiring. Auto-verify:
`git diff main...HEAD -- mobile/core/src/commonMain/kotlin/dev/horologia/mobile/core/feature/profile/ mobile/iosApp/iosApp/ProfileView.swift`
shows no content changes. ✓ DONE — neither `feature/profile/` nor `ProfileView.swift` changed on
this branch (verified via `git diff main...HEAD`).

**AC27** (Adversarial mitigation): `ApiBootstrapTest` regression guard asserts a baseUrl swap takes
effect. Auto-verify: `ApiBootstrapTest.configureHorologiaApi_rebasesOnRecall` passes. ✓ DONE —
regression test green; the `Url(...)`-rejection case was dropped because empty-string Url parses in
Ktor 3.4.2 (recorded in the test's doc comment).

---

## Implementation Plan

**Branch:** mobile/login-flow **Started:** 2026-04-21 **Base commit:**
a7a1e94e939084bed73169c6f65eff546a3bb803

### Task list

- [x] Update `mobile/gradle/libs.versions.toml` (deps: browser, security-crypto, ktor,
      serialization-json)
- [x] Update `mobile/core/build.gradle.kts` (new source sets + deps)
- [x] Edit `server/internal/database/migrations/00001_initial.sql` (seed horologia-mobile)
- [x] `:core/commonMain/session/SessionStore.kt` (expect class)
- [x] `:core/commonMain/session/ServerPrefs.kt` (expect class)
- [x] `:core/commonMain/session/SessionHolder.kt`
- [x] `:core/commonMain/platform/BrowserLauncher.kt` (expect class)
- [x] `:core/commonMain/platform/Crypto.kt` (expect object)
- [x] `:core/commonMain/feature/login/UrlNormalizer.kt`
- [x] `:core/commonMain/feature/login/Pkce.kt`
- [x] `:core/commonMain/feature/login/LoginUiState.kt`
- [x] `:core/commonMain/feature/login/LoginGateway.kt`
- [x] `:core/commonMain/feature/login/LoginViewModel.kt`
- [x] `:core/commonMain/feature/login/BootRouter.kt`
- [x] `:core/commonMain/ApiBootstrap.kt` (modify — doc comment)
- [x] `:core/commonMain/AppContainer.kt` (modify — add factories + platform ctor)
- [x] `:core/androidMain/session/SessionStore.android.kt`
- [x] `:core/androidMain/session/ServerPrefs.android.kt`
- [x] `:core/androidMain/platform/Crypto.android.kt`
- [x] `:core/androidMain/platform/BrowserLauncher.android.kt`
- [x] `:core/desktopMain/session/SessionStore.desktop.kt`
- [x] `:core/desktopMain/session/ServerPrefs.desktop.kt`
- [x] `:core/desktopMain/platform/Crypto.desktop.kt`
- [x] `:core/desktopMain/platform/BrowserLauncher.desktop.kt`
- [x] `:core/iosMain/session/SessionStore.ios.kt`
- [x] `:core/iosMain/session/ServerPrefs.ios.kt`
- [x] `:core/iosMain/platform/Crypto.ios.kt`
- [x] `:core/iosMain/platform/BrowserLauncher.ios.kt`
- [x] `:core/commonTest/feature/login/UrlNormalizerTest.kt`
- [x] `:core/commonTest/feature/login/PkceTest.kt`
- [x] `:core/commonTest/feature/login/LoginGatewayTest.kt`
- [x] `:core/commonTest/feature/login/LoginViewModelTest.kt`
- [x] `:core/commonTest/feature/login/BootRouterTest.kt`
- [x] `:core/commonTest/ApiBootstrapTest.kt`
- [x] `:core/commonTest/platform/Fakes.kt`
- [x] `:compose-app/commonMain/nav/Routes.kt` (modify — add LoginRoute)
- [x] `:compose-app/commonMain/HorologiaApp.kt` (modify — startDestination param)
- [x] `:compose-app/commonMain/theme/HorologiaTheme.kt`
- [x] `:compose-app/commonMain/feature/login/LoginScreen.kt`
- [x] `:compose-app/androidMain/MainActivity.kt` (modify)
- [x] `:compose-app/androidMain/platform/AndroidBrowserLauncher.kt` (landed as
      `AndroidBrowserLauncherImpl.kt`; the commonMain interface `AndroidBrowserLauncher` lives in
      `:core`)
- [x] `:compose-app/androidMain/platform/OAuthRedirectActivity.kt`
- [x] `:compose-app/androidMain/AndroidManifest.xml` (modify)
- [x] `:compose-app/androidMain/res/xml/network_security_config.xml` (modify)
- [x] `:compose-app/desktopMain/Main.kt` (modify)
- [x] `:compose-app/build.gradle.kts` (modify — remove dev-token, add browser dep)
- [x] `iosApp/project.yml` (modify)
- [x] `iosApp/Config.xcconfig` (delete)
- [x] `iosApp/iosApp/HorologiaApp.swift` (rewrite)
- [x] `iosApp/iosApp/LoginView.swift` (new)
- [x] `iosApp/iosApp/OAuthLauncher.swift` (new)

### Pre-existing failures (do not fix — out of scope)

- `mobile/design/login-flow.html` — dprint flags two prose reflow diffs in the design-spec HTML.
  File is an untracked design artefact not landed by this task; leaving unformatted per the
  scope-discipline rule.
- `workpad.md` — dprint reflows bullet list wrapping in this workpad. Not part of mobile code;
  untouched.

---

## Completion Bar

_All 6 must be checked before handing off to review._

1. [x] All planned files created/modified (task list fully checked)
2. [x] Linter clean — zero new errors (pre-existing errors logged above)
3. [x] Tests pass — new failures logged as pre-existing above
4. [x] Every AC has a completion note
5. [x] No open markers remain (`[NEEDS CLARIFICATION]`, `[TODO]`, `[TODO-IMPL]`, `[TBD]`)
6. [x] Scope discipline honored — discovered improvements in `[DEFERRED]` items only

---

## Review

**Reviewers activated:** correctness, simplification, requirements, idioms (Kotlin), idioms (Swift),
security, memory-safety, tests, error-handling, api-surface, documentation, concurrency,
accessibility, prose, architecture.

**AC coverage:** 27/27 pass. Only advisory is AC22 (Liquid Glass simulator visual —
`[MANUAL-VERIFY]`).

### Findings (grouped by severity + corroboration)

#### P1 — must address before push

- **F-1 [CORROBORATED-6] `BootRouter.decideBootDestination` never produces `SignedOutAfterRefresh`;
  silent refresh is unwired.** `BootRouter.kt:46-55` returns `SignedIn` whenever a stored session
  exists, regardless of access-token expiry or refresh-token presence. No caller invokes
  `LoginGateway.refreshAccessToken`. R14's "success → Profile; failure → ServerPicker with banner
  'Signed out.'" branch is dead code — a stored expired token sends users to Profile, which then
  401s with no recovery. Flagged by simplification, tests, correctness, api-surface, documentation,
  error-handling. (human-triage)

- **F-2 [CORROBORATED-4] iOS `try!` + `DispatchSemaphore` on `HorologiaApp.init` — crash + deadlock
  risk.** `HorologiaApp.swift:67-77` uses `Task { }` inside `@MainActor init`; the Task inherits
  MainActor and awaits `container.bootRouter.decideBootDestination()`, but the main thread is
  blocked on `semaphore.wait(timeout: .seconds(3))` — Task can't resume on main actor, deadlocks
  until 3s timeout, then `result` falls through as `Unconfigured` (saved server silently discarded).
  Any thrown error crashes via `try!`. Flagged by Swift idioms, Kotlin idioms, correctness,
  concurrency. (auto-fix: switch to a SwiftUI root `@State` + `.task { … }` with loading splash;
  remove `try!`)

- **F-3 [CORROBORATED-2] Browser-launcher has no timeout on desktop or Android.**
  `BrowserLauncher.desktop.kt:35-70` and `AndroidBrowserLauncherImpl.kt:19` both `await` a
  `CompletableDeferred<String>` with no `withTimeout`. If the user abandons Custom Tabs
  (back-swipes) or closes the desktop browser tab, `flowJob` stays suspended forever holding PKCE
  state, UI is stuck on `LaunchingBrowser` with no cancel affordance. Flagged by correctness,
  error-handling. (auto-fix: wrap with `withTimeout(5.minutes)` + show a "Still signing in?" banner
  on expiry)

- **F-4 [CORROBORATED-3] Desktop `HttpServer` lifecycle bugs.** `BrowserLauncher.desktop.kt` (a)
  never removes its `/` context on success — retry within the 5-second shutdown window crashes with
  `context "/" already exists`; (b) `server = null` writes from a non-synchronized daemon thread
  while `ensureServer()` synchronizes reads — torn reads/writes; (c) `HttpServer.create` is not
  guarded — `BindException` propagates as a raw banner. Flagged by correctness (F7), concurrency
  (F3), error-handling (F4). (auto-fix: remove context on success, synchronize `server=null`, wrap
  create in try/catch)

- **F-5 [CORROBORATED-3] iOS Keychain leaks and unchecked return codes.**
  `SessionStore.ios.kt:84-100` `copyData` returns the `CFDataRef` from `SecItemCopyMatching` without
  `CFRelease` / `CFBridgingRelease` → leak per read; `SecItemAdd/Update/Delete` return codes (lines
  55-82) are discarded, so a locked-Keychain write appears to succeed. Flagged by memory-safety,
  Kotlin idioms, error-handling, concurrency. (auto-fix)

- **F-6 `OAuthResultChannel` arm/deliver/cancel race.** `OAuthRedirectActivity.kt:41-58` uses
  `@Volatile var pending` but read-then-act sequences are not atomic. Rapid retry after a cancelled
  flow can null a freshly-armed deferred; a late redirect after `LoginViewModel.flowJob.cancel()`
  can wipe the next in-flight flow. Flagged by concurrency (F1), correctness (F8), error-handling
  (F7). (auto-fix: guard with `Mutex` or `AtomicReference.compareAndSet`)

- **F-7 Android cold-launch `runBlocking` on main thread during `EncryptedSharedPreferences` init.**
  `MainActivity.kt:23` blocks on `bootRouter.decideBootDestination()`, which reaches
  `EncryptedSharedPreferences.create` — documented 200–800 ms first-run cost on older devices. ANR
  risk. Flagged by Kotlin idioms, Swift idioms (parallel), correctness (F13). (auto-fix: render
  splash composable first, then async-decide via `LaunchedEffect`)

- **F-8 `LoginViewModel.onSubmit` does not cancel `probeJob` before launching the flow.** A late
  probe completion during `LaunchingBrowser`/`Finishing` can race into the UiState. Guarded by
  `as? LoginUiState.ServerPicker ?: return` inside `runProbe` so it's currently defanged, but
  fragile. `LoginViewModel.kt:142-170`. (auto-fix: `probeJob?.cancel()` at top of `onSubmit`)

- **F-9 Stale `HOROLOGIA_DEV_TOKEN` / `Config.xcconfig` references in top-level `CLAUDE.md`.** The
  PR deletes both but CLAUDE.md still instructs contributors to edit `Config.xcconfig`. Flagged by
  documentation. (auto-fix)

#### P2 — address before push; candidates for human-triage

- **F-10 [CORROBORATED-4] `LoginViewModel.onExternalCallback` is a public no-op.** Speculative
  generality; misleading KDoc tells platform authors to forward `.onOpenURL` into it. Flagged by
  simplification, api-surface, documentation, architecture. (auto-fix: delete)

- **F-11 [CORROBORATED-2] `pendingNormalizedUrl` never cleared in the `finally` block.** Correctness
  F5 + security F5. Breaks the documented invariant that all `pending*` fields are cleared on
  terminal transition. (auto-fix)

- **F-12 [CORROBORATED-2] `ensureApiPath` duplicated in 3 places (Kotlin x2 + Swift).**
  `MainActivity.kt:73-77`, `Main.kt:61-72`, `HorologiaApp.swift:79-94`, and a separate `internal`
  copy in `AppContainer.kt:88`. Flagged by simplification, architecture. (auto-fix: expose a public
  helper from `:core` and have platforms call through)

- **F-13 [CORROBORATED-3] AppContainer `expect class` boilerplate.** Each actual re-declares 8
  getters as identical delegation; desktop + iOS files are byte-for-byte identical. New container
  fields require 3-file lockstep edits. Flagged by simplification, Kotlin idioms, architecture.
  (human-triage: refactor to `expect fun createPlatformDependencies` returning a data class consumed
  by a single common `AppContainer`, OR accept the current shape until the next field forces the
  issue)

- **F-14 [CORROBORATED-2] Seam interfaces `BrowserDriver` / `ServerPrefsReader` / `SessionPersister`
  are avoidable.** Each mirrors its expect-class counterpart method-for-method; with
  `-Xexpect-actual-classes` already enabled, expect-interfaces remove the adapter layer. Flagged by
  simplification, Kotlin idioms. (human-triage: expect-interfaces are novel in this codebase; accept
  or refactor)

- **F-15 Token-exchange classifier leaks raw `Throwable.message` to UI banner.**
  `LoginGateway.kt:193` + `LoginViewModel.kt:238`. Ktor exception messages can contain URLs, TLS
  details, certificate paths — and at least in theory fragments of the in-flight URL. Also collapses
  `UnknownHostException` and `SSLHandshakeException` (permanent) into `Retryable`. Flagged by
  security (F-new), error-handling (F2). (auto-fix: map exception types to fixed user-facing
  strings; log details separately)

- **F-16 Error banners expose HTTP status codes to users.** `LoginGateway.kt:184-185` renders
  `"Sign-in failed (${response.status.value})."` to the banner. Flagged by prose (F4). (auto-fix:
  drop the parenthetical; log the code)

- **F-17 Android `OAuthRedirectActivity` accepts any external intent with the scheme; any app can
  stuff `OAuthResultChannel`.** Mitigated by state validation (state-mismatch → banner) but enables
  DoS and — in collusion scenarios — redirect hijack. `AndroidManifest.xml:34-37`. Flagged by
  security (P1→P2 with state validation in place), correctness. (auto-fix: validate
  `intent.data.scheme == "horologia"` and `host == "oauth"` before delivery)

- **F-18 Desktop loopback handler doesn't validate request origin or pin the path.** Any request to
  `127.0.0.1:<port>` resumes the continuation — including the browser's speculative `favicon.ico`
  prefetch. `BrowserLauncher.desktop.kt:38`. Flagged by security (F2/F3). (auto-fix: check
  `exchange.remoteAddress.address.isLoopbackAddress`; ignore requests that don't carry
  `code=`/`state=` query params)

- **F-19 `SessionHolder.install` ordering + error behavior.** If `persister.write` throws (locked
  Keychain, disk full, bad master key), the user sees a raw crypto exception in the banner and no
  session is retained. Flagged by error-handling. (human-triage: decide whether memory-only fallback
  is acceptable; if not, map the exception to "Couldn't save your session.")

- **F-20 `UrlNormalizer` computes `Url(withoutTrailingSlash).toString()` then discards it —
  abandoned refactor.** `UrlNormalizer.kt:22-34`. The canonicalization is computed and thrown away;
  the raw input is returned instead. Flagged by correctness (F6). (auto-fix: pick one — canonicalize
  or remove the dead computation; update the tests accordingly)

- **F-21 `seedInitialUrl` re-debounces a known-valid URL for 600 ms.** The user sees a pre-filled
  URL that won't let them tap Continue for 600 ms. `LoginViewModel.kt:80-85`. (auto-fix: skip the
  debounce and call `runProbe` directly for seeded values)

- **F-22 `appendPath` malforms URLs that carry a query string.** `LoginGateway.kt:200-205` emits
  `https://host?q=1/app/auth/config`. Flagged by correctness (F15). (auto-fix: parse via
  `URLBuilder` or strip query/fragment in `UrlNormalizer`)

- **F-23 `horologia://oauth` constant declared in `:core/androidMain` AND `AndroidManifest.xml` with
  no cross-reference.** Silent-drift risk. Flagged by architecture (F3), documentation (F14).
  (auto-fix: cross-reference comment OR extract a shared constant + resource)

- **F-24 iOS `OAuthLauncher.currentSession` leaks + continuation race.**
  `OAuthLauncher.swift:14-51`. Session retained across calls, no `[weak self]` in completion
  handler, `withCheckedThrowingContinuation` has no `onCancel`. Flagged by Swift idioms, concurrency
  (F6). (auto-fix: clear `currentSession` in `defer`, add `onCancel` to handle Kotlin-side
  cancellation)

- **F-25 Silent Keychain write failure on first-boot-before-unlock on iOS.** `SecItemAdd` returns
  `errSecInteractionNotAllowed` when Keychain is locked (`kSecAttrAccessibleAfterFirstUnlock` needs
  first unlock). Signed-in state survives in memory but not on disk; next cold launch re-prompts. No
  user indication. Flagged by error-handling (F10). (auto-fix: surface write failure to UI or log +
  retry)

- **F-26 Android Compose field missing
  `KeyboardOptions(keyboardType = KeyboardType.Uri, autoCorrectEnabled = false, capitalization = KeyboardCapitalization.None, imeAction = ImeAction.Go)`.**
  iOS side has the equivalent set. Flagged by accessibility (F8). (auto-fix)

- **F-27 iOS `TextField` missing `.accessibilityLabel("Server URL")` — placeholder is the only
  label.** Flagged by accessibility (F5). (auto-fix)

- **F-28 Progress indicators (both platforms) have no `accessibilityLabel` / `contentDescription`.**
  Flagged by accessibility (F7). (auto-fix)

- **F-29 Banner is rendered as plain `Text` with no live-region / alert semantics and no dismiss
  affordance.** Screen readers won't announce failed sign-in banners. Flagged by accessibility (F2,
  F9, F10). (auto-fix: `LiveRegionMode.Assertive` + add optional dismiss icon)

- **F-30 Error state is color-only on iOS.** Flagged by accessibility (F3, F4). (auto-fix: add SF
  Symbol prefix)

- **F-31 LoginScreen uses `Color.red` literal instead of semantic
  `MaterialTheme.colorScheme.error`.** Flagged by accessibility (F3). (auto-fix)

- **F-32 No focus request on field first render.** Flagged by accessibility (F6). (auto-fix)

- **F-33 Test-coverage gaps: `refreshAccessToken` untested;
  `TokenResult.AuthFailure/Retryable/Permanent` branches in `onSubmit` untested;
  `BrowserCancelledException` path untested; `seedInitialUrl`/`showBanner`/`dismissBanner` untested;
  `appendPath`/`authorizeUrl` helpers untested; `exchangeCode` non-401 branches untested.** Flagged
  by tests. (auto-fix: add tests)

- **F-34 `LoginViewModelTest.finishingMinDwellIsHonored` asserts a permissive disjunction that
  passes too easily.** Flagged by tests. (auto-fix: split into `dwellsUntilMinimum` +
  `skipsDwellWhenAlreadyElapsed` with explicit virtual-time)

- **F-35 `LoginGatewayTest` uses `runBlocking` instead of `runTest`.** Flagged by tests. (auto-fix)

- **F-36 `ApiBootstrapTest` mutates `Api` global singleton with no `@AfterTest` cleanup — leaks
  state across tests.** Flagged by tests. (auto-fix)

- **F-37 Ktor `HttpClient` constructed and closed per `LiveLoginGateway` call — loses connection
  pooling.** Flagged by Kotlin idioms. (human-triage: keep a long-lived client in the gateway or
  accept the cost for a low-frequency path)

- **F-38 `HorologiaTheme` is a no-op pass-through today.** Flagged by simplification, architecture
  (naming: implies cross-platform design system that doesn't exist). (human-triage: delete OR keep
  as future-proofing with a TODO)

- **F-39 `AppContainer` Android actual receives `Context` via ctor but could also use
  `androidx.startup` Initializer; implementation chose ctor.** Flagged by architecture (comment).
  Accept as documented.

#### P3 — polish; worth a sweep

- **F-40 `@Suppress("UNUSED_VARIABLE") val _unused = stored` in `BootRouter.kt:53` and
  `@Suppress("UNUSED_PARAMETER") val ignored = callbackUrl` in
  `LoginViewModel.onExternalCallback`.** Non-idiomatic Kotlin. Flagged by Kotlin idioms,
  simplification. (auto-fix)

- **F-41 iOS `BrowserLauncher.Companion.installedLauncher` not `@Volatile` (Android variant is).**
  Flagged by Kotlin idioms, api-surface, concurrency, correctness. (auto-fix: add for parity)

- **F-42 Prose polish: "Opening secure sign-in…" drops "secure"; "Sign-in couldn't be verified." →
  "Sign-in failed. Try again." (active); "Request timed out after $tokenTimeout." → "The server took
  too long to respond. Try again.".** Flagged by prose (F2, F3, F5). (auto-fix)

- **F-43 KDoc grammar/link errors: `a "Opening..."` → `an "Opening…"`; `[Signed In]` link target is
  `SignedIn`; "always the verbatim" is redundant.** Flagged by prose (F6, F10, F11). (auto-fix)

- **F-44 Class-level `@Suppress("CAST_NEVER_SUCCEEDS", "UNCHECKED_CAST")` on `SessionStore.ios.kt`
  is too broad.** Flagged by Kotlin idioms. (auto-fix: narrow to the single `as` expression)

- **F-45 `LoginViewModel` has two overlapping `internal constructor` declarations.** Flagged by
  Kotlin idioms, architecture, api-surface. (human-triage: reshape test constructor as a companion
  `forTest(...)` factory)

- **F-46 LoginViewModel / LoginGateway `@OptIn(ExperimentalTime::class)` spreads despite only
  reading `Clock.System.now().toEpochMilliseconds()`.** Flagged by Kotlin idioms. (auto-fix: accept
  a `() -> Long` injected at call site OR accept the OptIn surface; one call)

- **F-47 PKCE verifier is at the minimum RFC 7636 length (32 bytes → 43 chars).** Info only; no
  change required.

- **F-48 `LoginGateway.refreshAccessToken`, `BootDestination.SignedOutAfterRefresh`,
  `LoginViewModel.onExternalCallback` are all unused; if silent-refresh remains deferred (F-1),
  consider also deleting these until the refresh task lands so the public surface doesn't carry dead
  branches.** Flagged by simplification (F7), api-surface (F5). (human-triage: paired with F-1
  decision)

- **F-49 `SessionStore.read`'s silent null-on-corruption behavior is undocumented in the expect
  KDoc.** Flagged by documentation (F8). (auto-fix)

### Reviewers with no material findings

None — all 15 reviewers produced findings. This is expected for a diff of this scope (3885 lines, 57
files, 3 platforms, new OAuth surface).

### Notes for refine phase

- **F-1 is the single highest-impact finding** (6 reviewers CORROBORATED). Resolving it forces a
  scope decision: either (a) wire silent refresh at cold-launch in this PR (moderate scope
  expansion), or (b) delete the dead branches (`SignedOutAfterRefresh`,
  `LoginGateway.refreshAccessToken`, `onExternalCallback`) and accept that
  cold-launch-with-expired-token routes to Profile → 401 → generic error until a dedicated refresh
  task lands. Option (b) is cleaner for this PR's scope; silent refresh becomes its own `/craft`
  task.
- **F-2 through F-8** are the must-fix P1 items that can all be auto-fixed.
- **F-10 through F-36 are a mix of auto-fix and human-triage.** Most auto-fixes are small mechanical
  changes; the human-triage items (F-13, F-14, F-19, F-37, F-38, F-45, F-48) are refactor decisions.
- **P3 items F-40 through F-49** are polish; can be batched as a single cleanup commit.

### Refine — auto-fixes applied

Applied (28 findings): F-2, F-3, F-4, F-5, F-6, F-7, F-8, F-9, F-10, F-11, F-12, F-15, F-16, F-17,
F-18, F-20, F-21, F-22, F-23, F-24, F-25 (via F-5 exception propagation), F-26, F-27, F-28, F-29,
F-30, F-31 (iOS already used semantic-ish `Color.red`; Compose had no `Color.red` literal to replace
— banner/error now use `MaterialTheme.colorScheme.error` as before), F-32, F-33, F-34, F-36, F-40,
F-41, F-42, F-43, F-44, F-49.

Deferred (preexisting + human-triage, untouched per scope): F-1, F-13, F-14, F-19, F-35, F-37, F-38,
F-45, F-46, F-48.

- **F-35 (runTest in LoginGatewayTest)** attempted but hit the pre-existing "Ktor timeout interacts
  with runTest virtual time" constraint recorded in Deferred Items. Kept `runBlocking` for gateway
  tests that rely on Ktor's real-time `withTimeout`; the new timeout tests use a 50-ms real-time
  `tokenTimeout` so `runBlocking` stays quick. Added [DEFERRED] note to pick back up once gateway
  accepts injected dispatcher.

---

## Deferred Items

_Accumulated across all phases._

- M3 Expressive API adoption when CMP catches up
  - severity: P3
  - phase: implement
  - context: CHANGELOG WebFetch confirmed JetBrains CMP 1.9.0 doesn't ship `MaterialExpressiveTheme`
    / wavy `LoadingIndicator`. `HorologiaTheme` wraps baseline `MaterialTheme` today; swap the body
    when a future CMP release ships the expressive API.
- androidx.security:security-crypto Keystore migration
  - severity: P2
  - phase: implement
  - context: `androidx.security:security-crypto` 1.1.0 is officially deprecated in favour of direct
    Android Keystore use. Shipped 1.1.0-alpha06 as a pragmatic stop-gap (comment in
    `SessionStore.android.kt` + `libs.versions.toml` records this). Replacing with a Keystore +
    hand-rolled AES/GCM is a follow-up.
- Real OS-keychain integration on desktop
  - severity: P3
  - phase: implement
  - context: Desktop `SessionStore` ships an AES-GCM file at the platform user-data dir (R12
    fallback clause). macOS Keychain / Windows Credential Manager / libsecret integrations are a
    follow-up.
- Silent-refresh mid-session interceptor
  - severity: P2
  - phase: architect
  - context: Architect-phase decision: cold-launch refresh is this task; mid-session 401 currently
    surfaces `AuthFailure` and the user re-signs-in.
- TanStack-Query-style data layer in :core
  - severity: P3
  - phase: architect
  - context: Explicitly deferred to a follow-up `/craft`. Would migrate
    `ProfileGateway`/`SpacesGateway` to a `Fetcher<Key, Value>` shape and retire the manual
    `StateFlow` plumbing.
- Sign-out UI on Profile
  - severity: P3
  - phase: architect
  - context: Profile redesign is the next task; sign-out goes there.
- Mobile observability (structured auth-flow logging)
  - severity: P3
  - phase: implement
  - context: `:core` has no logging facility today; auth events land with no telemetry. Add
    alongside a later observability pass.
- Ktor timeout interacts with `runTest` virtual time
  - severity: P3
  - phase: implement
  - context: Found while writing `LoginGatewayTest`: `withTimeout(4.seconds)` inside `runTest`
    triggers virtual-time cancellation even when the MockEngine responds eagerly. Tests use
    `runBlocking` as a workaround; a follow-up could teach the gateway to accept an injected
    `CoroutineContext` so tests can drop real-time waits entirely.
- F-35: LoginGatewayTest runTest migration
  - severity: P3
  - phase: refine
  - context: Per the existing deferred item above — `runTest` + Ktor `withTimeout` can't be
    reconciled without injecting a `CoroutineContext` into `LiveLoginGateway`. Keep `runBlocking`
    - short real-time timeouts in the new gateway tests until the injection change lands.
- F-37 — Long-lived HttpClient in LiveLoginGateway
  - severity: P2
  - phase: review
  - context: Login is low-frequency; per-call HttpClient construction cost is acceptable until other
    `:core` features consume the same Ktor direct-call pattern. Revisit when a second caller lands.
- F-46 — Consolidate @OptIn(ExperimentalTime) surface
  - severity: P3
  - phase: review
  - context: Human accepted either outcome; left as-is. Experimental opt-in is a conventional Kotlin
    idiom and doesn't materially harm readability.
- F-19 — Memory-only session fallback on SessionStore.write failure
  - severity: P2
  - phase: review
  - reason: REJECTED — user chose to leave fail-closed behavior. Keychain/SharedPrefs write failures
    surface as a banner and block the flow.

---

## Phase Log

<!-- Format: - [phase]: [status] — [ISO date] — [notes] -->

- explore: done — 2026-04-21 — 5 explorers (arch, change-surface, history, data/state, deps). Key
  reframe: OAuth+PKCE is already live server-side; CLI has working prior-art at `cli/`. Outstanding
  decisions surfaced for clarify/architect: custom-scheme vs loopback redirect on mobile (server
  matcher is loopback-only today), cache warm-up is aspirational (no cache exists), CMP M3
  Expressive availability unverified, new OAuth client seed vs reuse of `horologia-cli`.
- clarify: done — 2026-04-21 — 4 questions, all answered. Q4 flipped: hard-bump iOS 26 (user has
  Xcode 26). 27 requirements recorded.
- architect: done — 2026-04-21 — Composite plan (Pragmatic core + Robust edges) approved. P0-BLOCKER
  adversarial claim refuted via Ktor 3.4.2 bytecode verification; demoted to P2 with regression test
  guard. DB migration squashed per pre-prod convention.
- implement: done — 2026-04-21 — mobile/login-flow at 928bf2697385d69ff95c0683f2362547f263f0b2. 11
  commits (10 feature + 1 workpad). All 27 ACs have completion notes; `mise run //mobile:test` green
  (34 tests); commonMain / android / desktop / iosSimulatorArm64 / compose-app-debug-android /
  compose-app-desktop all compile clean; `mise run //mobile:ios:gen` regenerates the Xcode project.
  Pre-existing dprint formatting noise in `mobile/design/login-flow.html` + `workpad.md` logged
  under Pre-existing failures. Branch NOT pushed per pipeline convention.
- review: done — 2026-04-21 — 15 reviewers, 49 findings (9 P1, ~30 P2, ~10 P3). Top finding
  [CORROBORATED-6]: BootRouter silent refresh unwired. AC coverage 27/27 pass.
- refine:
- pr:

// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: recipe_ingredients.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const deleteRecipeIngredientSections = `-- name: DeleteRecipeIngredientSections :exec
DELETE FROM recipe_ingredient_sections
WHERE recipe_id = $1
`

func (q *Queries) DeleteRecipeIngredientSections(ctx context.Context, recipeID int64) error {
	_, err := q.db.Exec(ctx, deleteRecipeIngredientSections, recipeID)
	return err
}

const insertRecipeIngredient = `-- name: InsertRecipeIngredient :exec
INSERT INTO recipe_ingredients (
    section_id,
    position,
    quantity,
    quantity_max,
    unit,
    item,
    preparation,
    optional
)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
`

type InsertRecipeIngredientParams struct {
	SectionID   int64
	Position    int32
	Quantity    pgtype.Numeric
	QuantityMax pgtype.Numeric
	Unit        string
	Item        string
	Preparation string
	Optional    bool
}

func (q *Queries) InsertRecipeIngredient(ctx context.Context, arg InsertRecipeIngredientParams) error {
	_, err := q.db.Exec(ctx, insertRecipeIngredient,
		arg.SectionID,
		arg.Position,
		arg.Quantity,
		arg.QuantityMax,
		arg.Unit,
		arg.Item,
		arg.Preparation,
		arg.Optional,
	)
	return err
}

const insertRecipeIngredientSection = `-- name: InsertRecipeIngredientSection :one
INSERT INTO recipe_ingredient_sections (recipe_id, title, position)
VALUES ($1, $2, $3)
RETURNING id, recipe_id, title, position
`

type InsertRecipeIngredientSectionParams struct {
	RecipeID int64
	Title    string
	Position int32
}

func (q *Queries) InsertRecipeIngredientSection(ctx context.Context, arg InsertRecipeIngredientSectionParams) (RecipeIngredientSection, error) {
	row := q.db.QueryRow(ctx, insertRecipeIngredientSection, arg.RecipeID, arg.Title, arg.Position)
	var i RecipeIngredientSection
	err := row.Scan(
		&i.ID,
		&i.RecipeID,
		&i.Title,
		&i.Position,
	)
	return i, err
}

const listRecipeIngredientRows = `-- name: ListRecipeIngredientRows :many
SELECT
    ris.id AS section_id,
    ris.title AS section_title,
    ris.position AS section_position,
    ri.id AS ingredient_id,
    ri.position AS ingredient_position,
    ri.quantity,
    ri.quantity_max,
    ri.unit,
    ri.item,
    ri.preparation,
    ri.optional
FROM recipe_ingredient_sections ris
LEFT JOIN recipe_ingredients ri ON ri.section_id = ris.id
WHERE ris.recipe_id = $1
ORDER BY ris.position, ri.position
`

type ListRecipeIngredientRowsRow struct {
	SectionID          int64
	SectionTitle       string
	SectionPosition    int32
	IngredientID       pgtype.Int8
	IngredientPosition pgtype.Int4
	Quantity           pgtype.Numeric
	QuantityMax        pgtype.Numeric
	Unit               pgtype.Text
	Item               pgtype.Text
	Preparation        pgtype.Text
	Optional           pgtype.Bool
}

func (q *Queries) ListRecipeIngredientRows(ctx context.Context, recipeID int64) ([]ListRecipeIngredientRowsRow, error) {
	rows, err := q.db.Query(ctx, listRecipeIngredientRows, recipeID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListRecipeIngredientRowsRow{}
	for rows.Next() {
		var i ListRecipeIngredientRowsRow
		if err := rows.Scan(
			&i.SectionID,
			&i.SectionTitle,
			&i.SectionPosition,
			&i.IngredientID,
			&i.IngredientPosition,
			&i.Quantity,
			&i.QuantityMax,
			&i.Unit,
			&i.Item,
			&i.Preparation,
			&i.Optional,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
